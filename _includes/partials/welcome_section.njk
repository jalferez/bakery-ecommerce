<section class="welcome-section">
  <div class="welcome-content">
    <h2>Welcome to your local bakery café</h2>
    <h3>Baked with love, rooted in flavor.</h3>
    <p>
      At Treetop Bakery, every loaf, pastry, and brew is crafted with care.
      We believe in simple ingredients, warm smiles, and the aroma of fresh bread that feels like home.
    </p>
  </div>

  <div class="welcome-video">
    <video autoplay muted loop playsinline>
      <source src="/videos/TB_vid.mp4" type="video/mp4" />
      Your browser does not support the video tag.
    </video>
  </div>

  <style>
    .welcome-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 3rem 1rem;
      background-color: #fffaf5;
      color: #3b1b14;
      overflow: hidden;
      gap: 2rem;
    }

    .welcome-content h2 {
      font-family: "Playfair Display", serif;
      font-weight: 700;
      font-size: clamp(2rem, 4vw + 1rem, 3.5rem);
      color: #004F2D;
      margin-bottom: 0.6rem;
      letter-spacing: 0.5px;
    }

    .welcome-content h3 {
      font-family: "Sora", sans-serif;
      font-weight: 600;
      font-size: clamp(1.2rem, 2vw + 0.8rem, 2rem);
      color: #895129;
      margin-bottom: 1rem;
    }

    .welcome-content p {
      font-family: "Sora", sans-serif;
      font-size: clamp(1rem, 1vw + 0.8rem, 1.3rem);
      line-height: 1.8;
      max-width: 700px;
      margin: 0 auto;
      color: #4b2e22;
    }

    .welcome-video {
      width: 100%;
      max-width: 1100px;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
    }

    .welcome-video video {
      width: 100%;
      height: 250px;
      object-fit: cover;
      object-position: center;
      display: block;
    }

    /* Medias */

    @media (min-width: 768px) {
      .welcome-section {
        padding: 5rem 2rem;
        gap: 3rem;
      }

      .welcome-video video {
        height: 380px;
      }
    }


    @media (min-width: 1024px) {
      .welcome-section {
        flex-direction: row;
        justify-content: center;
        align-items: center;
        gap: 4rem;
        text-align: left;
      }

      .welcome-content {
        flex: 1;
        max-width: 600px;
      }

      .welcome-video {
        flex: 1;
        max-width: 600px;
        height: 400px;
      }

      .welcome-video video {
        height: 100%;
      }
    }
  </style>
</section>